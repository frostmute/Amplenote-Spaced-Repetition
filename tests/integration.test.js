const fs = require('fs'); const path = require('path'); const MockApp = require('./mock-app'); const pluginContent = fs.readFileSync(path.join(__dirname, '../plugin.js'), 'utf8'); const plugin = eval(pluginContent); describe('Integration Tests', () => { it('should run an empty review session gracefully', async () => { const app = new MockApp(); app.prompt = async () => 'srs/review'; app.filterNotes = async () => [{ uuid: 'note-1' }]; app.notes = { find: async () => ({ uuid: 'note-1', content: async () => 'No flashcards here' }) }; let alerted = false; app.alert = async (msg) => { alerted = true; }; await plugin.noteOption['Start Review Session'].bind(plugin)(app, 'note-1'); expect(alerted).toBe(true); }); });
